SINGLE_ARM_ROBOT_PROMPT = """
该机器人为固定底座单机械臂操作机器人，由一条机械臂和一个夹爪或灵巧手组成，所有机器人操作均由该机械臂完成。

视频中可能仅显示末端执行器或局部连杆，机械臂也可能暂时被遮挡或离开画面。同一连续或可通过前后运动恢复的末端轨迹始终属于同一个执行主体，不得因重新进入画面而创建新的 executor。

分析视频时，只根据机械臂的末端运动、末端姿态、夹爪状态，以及其与物体之间的接近、接触、分离和同步运动关系判断动作。不得根据任务类型、机器人能力、操作意图或最终结果补全未直接观察到的动作。

action_sequence 中 executor 固定使用 "single"。action 按时间顺序记录该机械臂实际执行的基础动作或原子动作；object 记录该动作直接接近、接触、抓持、移动或释放的对象，没有明确对象时为空字符串 ""。
"""


BIMANUAL_ROBOT_PROMPT = """
该机器人为固定底座双机械臂操作机器人，由物理身份固定的左机械臂和右机械臂组成。两个机械臂可以独立、交替或同时参与任务，但只能根据视频中的直接视觉证据判断实际发生的动作，不得根据任务类型或机器人能力补全未观察到的动作。

【左右臂身份】

- executor="left" 表示机器人自身的左机械臂，executor="right" 表示机器人自身的右机械臂。
- 左右机械臂的身份在整段视频中保持固定，不因遮挡、离开画面、运动到另一侧、双臂交叉或当前画面位置变化而改变。
- 视频中左右机械臂可能不会同时出现，也可能仅显示末端执行器或局部连杆。
- 当画面中仅出现一个末端执行器时，不得默认其为 right，也不得因为另一机械臂尚未出现而重新分配身份。

【进入方向先验】

在当前固定摄像机视角下：

- 从画面左侧进入操作视野的末端执行器，优先识别为 left。
- 从画面右侧进入操作视野的末端执行器，优先识别为 right。

进入方向仅用于首次建立身份。不得根据末端执行器后续位于画面左侧或右侧重新判断身份；左机械臂可以运动到画面右侧，右机械臂也可以运动到画面左侧。

【轨迹连续性】

- 同一连续可见的末端执行器轨迹必须保持同一 executor。
- 末端执行器短暂遮挡或离开画面后重新出现时，应根据出现时间、空间位置、运动方向、末端姿态和夹爪状态的连续性恢复原身份。
- 不得把一条已确定为 left 的轨迹在后续改为 right，也不得把已确定为 right 的轨迹改为 left。
- 当进入方向和轨迹连续性均不足以判断身份时，应标记身份不确定，不得默认输出 right。

【动作分析】

分析视频时，应分别追踪左机械臂和右机械臂，只根据各自末端运动、末端姿态、夹爪状态以及与物体之间的距离、接触、分离和同步运动关系判断动作。

不得根据另一机械臂的运动补全当前机械臂的动作，也不得因为两个机械臂共同操作同一物体而合并其动作。

action_sequence 中 executor 仅使用 "left" 或 "right"，不使用 "both"。即使左右臂同时执行相同动作，也应分别输出两条记录。

action 记录对应机械臂实际执行的动作，object 记录该动作直接作用或朝向的对象；没有明确对象时为空字符串 ""。
"""


MOBILE_MANIPULATOR_PROMPT = """
该机器人为移动操作机器人，由移动底盘和一个或多个机械臂组成。机器人不仅能够移动到目标位置，还能够利用机械臂完成各种操作任务，因此完整任务通常包含移动与操作两个阶段。
机器人能够完成导航、定位、接近目标、抓取、搬运、运输、放置、推动、拉动、开关门、开关抽屉、插入、取出等操作，并能够在不同位置之间移动完成长程操作任务。
分析视频时，应将机器人行为理解为移动与操作相结合的连续过程，区分导航阶段和操作阶段，重点关注机器人何时开始与物体交互、如何完成操作目标、物体状态如何发生变化，以及不同任务阶段之间的切换，而不是关注底盘运动或机械臂关节运动等底层控制细节。
理解 action_sequence 时，底盘移动或接近目标阶段的 executor 使用 "base"，机械臂操作阶段的 executor 使用 "arm"；action 按时间顺序记录导航、接近、抓取、搬运、放置等高层动作；object 对于导航动作表示目标位置或目标区域，对于操作动作表示被操作对象，必须使用自然中文名称，如果没有明确对象则为空字符串 ""。
"""


ROBOT_TYPE_PROMPTS = {
    "single_arm": SINGLE_ARM_ROBOT_PROMPT,
    "bimanual": BIMANUAL_ROBOT_PROMPT,
    "mobile_manipulator": MOBILE_MANIPULATOR_PROMPT,
}


def get_robot_type_prompt(robot_type: str) -> str:
    prompt = ROBOT_TYPE_PROMPTS.get(robot_type)
    if prompt:
        return prompt.strip()
    return (
        '当前 robot_type 为 "unknown"。不要猜测机器人类别，只根据视频中可见的动作完成标注；'
        'executor 使用 "unknown"，action 按时间顺序记录可见高层动作，object 使用自然中文名称记录明确对象或空字符串 ""。'
    )